<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FallDetectionApp.ViewModels"

             
             mc:Ignorable="d"
             x:Class="FallDetectionApp.Views.GeoDataPage">

    <!--xmlns:vm="clr-namespace:FallDetectionApp.ViewModels"-->

    <!--<ContentPage.BindingContext>
        <vm:GeoDataViewModel />
    </ContentPage.BindingContext>-->

    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Text="+" Clicked="OnItemAdded">
            <ToolbarItem.IconImageSource>
                <OnPlatform x:TypeArguments="ImageSource">
                    <On Platform="Android, UWP" Value="plus.png" />
                </OnPlatform>
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>-->

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentPage.Resources>


    <ContentPage.BindingContext>
        <vm:GeoDataViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <Label Text="GeoLocation database"
               FontSize="Medium"
               TextColor="{StaticResource Primary}"
               VerticalOptions="CenterAndExpand" 
               HorizontalOptions="CenterAndExpand" />
        <!--
        <Button Text="Save" Clicked="SaveGeoLocationItem" />
        <Label Text="         "   d:Text="Latitude:" />
         -->
        <Button HeightRequest="150" x:Name="btnActivate" Text="{Binding btnActivateTxt}" Command="{Binding ToggleDidYouFall}"  />

        <!--x:Name="geoDataListView"       Command="{Binding OpenGeoDatabase}" IsRefreshing="{Binding IsBusy, Mode=OneWay}"  CachingStrategy="RecycleElement"  RefreshCommand="{Binding RefreshListView}"
                IsPullToRefreshEnabled="true"-->
        <ListView x:Name="geoItems" Margin="20" ItemSelected="OnListItemSelected" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="20,0,0,0" Orientation="Vertical" >
                            <Label Text="{Binding Id}" VerticalTextAlignment="Center" TextColor="{StaticResource Primary}" FontSize="Medium" FontAttributes= "Bold" HorizontalOptions="StartAndExpand" />
                            <StackLayout  Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                                <Label Margin="0,0,65,0" Text="{Binding Latitude}" FontSize="Medium" FontAttributes= "Bold" d:Text="Latitude:" />
                                <Label Text="{Binding Longitude}" FontSize="Medium" FontAttributes= "Bold" d:Text="Longitude:" />
                            </StackLayout>
                            <Label Text="{Binding Info}" d:Text="GeoInfo" />
                            <Label Text="{Binding TimeDate}" d:Text="GeoTimeDate" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <Button Text="Refresh List" HeightRequest="150" Clicked="UpdateDataListView" />
    </StackLayout>


</ContentPage>